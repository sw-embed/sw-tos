Emit build/storage-layout.json from the built storage image in the COLUMNAR contract pinned by ../../sw-ml-study/sw-mlpl/docs/storage-layout-viz.md: spaces/space_name/space_block/space_capacity (length S) and index-aligned region_space/region_kind/region_name/region_owner/region_start/region_length/region_text_words/region_data_words/region_bss_words (length N). Read every figure through cor24-storage.py and cor24-image.py rather than recomputing it. Keep padding distinct from free so block-alignment cost stays visible. Add a contract test that checks alignment, tiling and totals -- not the numbers -- and register it in the acceptance gate.