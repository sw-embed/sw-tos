Emit build/runtime-layout.json: the same columnar contract for the running system. Add spaces for EBR and system RAM, and region kinds for a process image, its stack, its state record, the executable boundary, the high-water mark and reclaimed space. Take the figures from the process table the monitor already reads so a snapshot and `mon` cannot disagree. Phase 2 of ../../sw-ml-study/demo-extensions/docs/research.txt. Extend the contract test to cover the new spaces and kinds.